[package]
name = "shuffle_gui"
version.workspace = true
edition = "2021"

[lints]
workspace = true

[dependencies]
egui = "0.31.1"  # Check for the latest version
eframe = "0.31.1"  # Check for the latest version
image = "0.25.6"
deadpool = { path = "../deadpool" }
